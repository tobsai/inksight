/**
 * Unit tests for reMarkable binary parser
 *
 * The skeleton RMParser (src/parser/rm-parser.ts) was removed — it was a
 * full-stub Phase 1.2 placeholder with no real implementation.
 * The real parser is src/renderer/rm-parser.ts (parseRMFile).
 * Tests for that live in src/renderer/renderer.test.ts.
 */

import { describe, it, expect } from 'vitest';

describe('Parser (placeholder)', () => {
  it('stub test — real rm parsing covered in renderer.test.ts', () => {
    expect(true).toBe(true);
  });
});
